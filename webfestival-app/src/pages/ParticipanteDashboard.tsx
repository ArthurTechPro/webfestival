import React from 'react';
import { DashboardParticipante } from '../components/pages/participant';

/**
 * Dashboard principal para participantes (artistas creativos)
 * Utiliza el nuevo componente DashboardParticipante implementado
 */
const ParticipanteDashboard: React.FC = () => {
  return <DashboardParticipante />;
};

export default ParticipanteDashboard;